1. When opening localhost:4200, it should get redirected to /home route. 
2. All other routes should be handled appropriately and redirected to root.
3. In the form, remove the ID field text box. ID should automatically get generated by incrementing last ID of the existing data
4. Form should get cleared after submitting
5. Make all the fields as required.
6. Display "NA" for the fields where data is not available.
7. Add Delete icon for each row. On confirming, delete that specific row.
